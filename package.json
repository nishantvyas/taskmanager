{
  "name": "task-manager-extension",
  "version": "1.2.3",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "build:chrome": "npm run build && node scripts/build-chrome.js",
    "zip": "npm run build:chrome",
    "version:sync": "node scripts/sync-version.js"
  },
  "dependencies": {
    "date-fns": "^2.30.0"
  },
  "devDependencies": {
    "vite": "^5.0.0",
    "archiver": "^6.0.1"
  }
} 